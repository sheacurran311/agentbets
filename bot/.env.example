# AgentBets X Bot Configuration
# ================================
# Copy this file to .env and fill in the values
# For Railway: Set these as service environment variables

# Bot Server
BOT_PORT=3003
NODE_ENV=development

# Data persistence directory (optional, defaults to ./data)
# DATA_DIR=/app/data

# ============================================
# REQUIRED - Bot will not work without these
# ============================================

# AgentBets API URL (required for creating markets and proposing resolutions)
AGENTBETS_API_URL=http://localhost:3002/api

# AgentBets API Key (REQUIRED - must match AGENTBETS_API_KEY in API server)
# This protects the API from spam and unauthorized access
# Generate the same key: openssl rand -hex 32
AGENTBETS_API_KEY=your-secret-api-key-here

# Twitter/X API Bearer Token (REQUIRED for follower verification)
# Get this from https://developer.twitter.com/en/portal/projects
# Without this, bets about follower counts CANNOT be verified!
TWITTER_BEARER_TOKEN=your-bearer-token

# ============================================
# REQUIRED FOR POSTING TWEETS
# ============================================

# Twitter OAuth 1.0a credentials (required for posting tweets)
TWITTER_API_KEY=your-api-key
TWITTER_API_SECRET=your-api-secret
TWITTER_ACCESS_TOKEN=your-access-token
TWITTER_ACCESS_SECRET=your-access-secret

# ============================================
# OPTIONAL
# ============================================

# AgentBets frontend URL (for generating market links)
AGENTBETS_URL=https://agentbets.gg

# ============================================
# INFERENCE.SH CLI (Alternative Twitter Backend)
# ============================================
# Use inference.sh CLI for posting tweets instead of twitter-api-v2
# Enables enhanced features: media posting, DMs, following
# Requires: curl -fsSL https://cli.inference.sh | sh && infsh login
USE_INFSH=false

# Moltbook Integration
# Register bot at POST https://www.moltbook.com/api/v1/agents/register
# Then set the API key here to enable Moltbook polling and posting
MOLTBOOK_API_URL=https://www.moltbook.com/api/v1
MOLTBOOK_BOT_API_KEY=your-moltbook-bot-api-key

# ============================================
# MOLTX INTEGRATION (moltx.io - X for AI agents)
# ============================================
# Register: POST https://moltx.io/v1/agents/register
# Claim:    node scripts/claim-moltx.js  (requires X account 10+ days old)
# Wallet:   node scripts/claim-moltx.js --wallet  (after generating EVM keypair)

# API key from registration response
MOLTX_API_KEY=moltx_sk_your-key-here

# Claim code from registration response (expires 24h — re-register if expired)
MOLTX_CLAIM_CODE=your-claim-code-here

# Bot name on MoltX (auto-detected at startup via getMe(), but can override)
# MOLTX_BOT_NAME=AgentBetsBot

# Custom MoltX API base URL (optional, defaults to https://moltx.io/v1)
# MOLTX_API_URL=https://moltx.io/v1

# EVM private key for wallet linking (REQUIRED for posting after claim).
# This is used for off-chain EIP-712 signing only — no transactions, no gas.
# Generate a dedicated wallet: node -e "const {Wallet}=require('ethers');const w=Wallet.createRandom();console.log(w.privateKey,w.address)"
# Store securely — wallet receives $USDC rewards from MoltX.
MOLTX_EVM_PRIVATE_KEY=0x-your-evm-private-key-here

# EVM chain ID for wallet linking (default: 8453 = Base)
# MOLTX_EVM_CHAIN_ID=8453

# GitHub API (for GitHub-based resolutions)
GITHUB_TOKEN=your-github-token

# CoinGecko API key (for token price resolution)
COINGECKO_API_KEY=your-coingecko-api-key

# Announce resolution proposals on Twitter (true/false)
ANNOUNCE_PROPOSALS=true
